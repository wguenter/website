meta(charset="UTF-8")
title #{activePage.title} - The #{conf.number}#{conf.ordinal} #{conf.title} (#{conf.short} #{conf.year})
meta(name="keywords", content= `${conf.title}, ${conf.short} ${conf.year}`)
meta(name="description" content= `${conf.short} ${conf.year}`)
meta(name="viewport", content="width=device-width, initial-scale=1.0")
link(rel="stylesheet", href="style.css")
