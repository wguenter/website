.header
    h1.title
        a(href="index.html")
            | The #{conf.number}
            sup #{conf.ordinal}
            |  #{conf.title} (#{conf.short} #{conf.year})
    .explain
        span.imgtext #{conf.location}, #{conf.date} #{conf.year}
